import { UrlConverterPipe } from './url-converter.pipe';

describe('UrlConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
